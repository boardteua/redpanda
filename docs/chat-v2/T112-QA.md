# T112 — QA: «Користувачі онлайн» у шапці вітальні

**Задача:** [project-tasks/chat-v2-tasklist.md](../../project-tasks/chat-v2-tasklist.md) — T112.

## Зміни

- `backend/resources/js/views/AuthWelcome.vue`: лічильник у блоці `.rp-auth-landing-brand-leading` одразу після логотипа; `v-if="!user"`; `role="status"`, `aria-live="polite"`, `aria-atomic="true"`.
- `backend/resources/css/app.css`: `.rp-auth-landing-brand-leading`, `.rp-auth-landing-online-header`; прибрано нижній футер `.rp-auth-landing-online`.

## Автоматичні перевірки

- `cd backend && npm run build` — PASS
- `cd backend && php artisan test` — **320 passed** (регресія не очікується)

## Ручний чек (оператор)

1. Відкрити `/` неавторизовано: у білій картці в шапці видно «Користувачі онлайн» і число **праворуч від** логотипа.
2. Унизу картки **нема** другого такого блоку.
3. Вузький viewport: без горизонтального скролу; при потребі блок переноситься (`flex-wrap`).
4. Після ~45 с poll оновлює число без помилок у консолі (як раніше `fetchLandingPublic`).

## Вердикт

**PASS** — після виконання команд вище та ручного чеку.
