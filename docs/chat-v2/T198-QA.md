# T198 — QA (глобальне LLM OFF: кімнатні налаштування панди)

## Автоматичні перевірки

- `php artisan test tests/Feature/RoomCrudApiTest.php` — у т.ч.:
  - `test_chat_admin_cannot_patch_ai_bot_enabled_true_when_global_llm_disabled`
  - `test_chat_admin_can_patch_ai_bot_enabled_true_when_global_llm_enabled`
- `npm run build` (каталог `backend/`) — без помилок збірки.

## Ручний сценарій (адмін чату)

1. У «Налаштуваннях чату» вимкнути глобальний LLM → відкрити «Редагувати кімнату» → переконатися, що **немає** блоку «Розумна панда» / `ai_bot_enabled`.
2. Увімкнути глобальний LLM → той самий модал → блок **знову видимий**; зберегти перемикач працює як раніше.

**PASS:** 2026-04-03 (автотести + збірка; ручний сценарій за потреби оператора).
