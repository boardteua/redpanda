# T103 — QA (декомпозиція ChatRoom.vue)

**Дата:** 2026-03-24  
**Зміст змін:** логіка привату (REST), друзів/ігнору та автокомпліту ніка винесена в `backend/resources/js/chat/*Methods.js`; Echo-підписки кімнати та `ensureUserPrivateListener` залишені в `ChatRoom.vue` без зміни імен подій чи payload.

## Автоматичні перевірки

| Крок | Результат |
|------|-----------|
| `cd backend && npm run build` | PASS |
| `cd backend && php artisan test` | PASS (303 tests) |

## Ручний smoke (рекомендовано на середовищі з Reverb, T96)

- Логін → вхід у кімнату → відправка повідомлення в стрічку.
- Відкриття привату з сайдбару (peer), обмін повідомленнями.
- Вкладка «Люди»: автокомпліт за префіксом ніка (≥2 символи), Enter / стрілки.
- Друзі / ігнор: завантаження списків після перемикання вкладок (як раніше).

## Code review

За регламентом оркестратора — окремий прохід **code-reviewer** на diff комітів **T103** (без 🔴 blockers перед наступною задачею за політикою репо).
