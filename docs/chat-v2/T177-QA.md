# T177 — QA

- **Tests (task-scoped)**:
  - `cd backend && php artisan test --filter Gemini` — **PASS**
  - Якщо змінювали `.env` і tinker/worker “не бачить” оновлень — виконайте `cd backend && php artisan config:clear` (або видаліть `backend/bootstrap/cache/config.php`).

- **Coverage**:
  - `GeminiClient` робить `POST` на `https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent` з `x-goog-api-key` (HTTP фейки, без мережі).
  - Queue job на 429/`RESOURCE_EXHAUSTED` **падає** (щоб черга застосувала `backoff()`/retries), інші помилки не замовчуються; structured-log метадані без user text.

