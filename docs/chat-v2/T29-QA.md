# T29 — QA: двохзонний композер і тулбар (legacy-подібно)

## Зміни

- **Верхній ряд** (`CHAT-MAIN-INPUT.md` §панель над полем): заглушки **колір тла / колір тексту**, **B / I / U / S**, **група** (усі disabled для T30); після spacer — **архів** (`router-link` на `archive` з `room` у query), **мої зображення** та **чат-рулетка** (disabled, T31 / майбутнє), **відкрити панель чату** (`beginOpeningPanel`).
- **Нижній ряд**: **смайл** (disabled, T33), **textarea** без накладених кнопок (оновлені відступи в CSS), **зображення** (як у T10), **велика кнопка «Надіслати»** з підписом від ~480px (`rp-chat-send-primary`), іконка лишається на вузьких екранах; `aria-label` на submit; видимий текст з `aria-hidden` для уникнення дубля в SR.
- Видалено невикористані стилі `rp-chat-composer-inner-btn` / `rp-chat-send-fab`.

## Ручний чекліст (оператор)

- **Tab:** верхній тулбар зліва направо → смайл → поле → фото → надіслати; `focus-visible` на кнопках і полі.
- Відправка тексту / зображення — без регресії; **Enter / Shift+Enter** (T28) у полі.
- Іконка архіву веде в архів з поточною кімнатою в query (як лінк у хедері).
- Кнопка панелі відкриває сайдбар (узгоджено з хедером).

## Автоматичні перевірки

- `npm run build` — PASS
- `php artisan test` — 93 passed

**Статус:** PASS
