# T166 — QA (Web Push: SW deep link)

**Статус:** PASS (2026-03-29), автоматизовано; клік по сповіщенню на пристрої — ручний smoke на HTTPS.

## Команди

- `cd backend && npm run build` — **PASS** (`public/build/sw.js` з оновленим `notificationclick`)
- `cd backend && php artisan test --filter='(PushSubscriptionApiTest|PrivateMessageApiTest|ChatApiTest)'` — **PASS**

## Що зроблено

| Критерій | Результат |
|----------|-----------|
| Кімната | Payload уже містить `data.url` з канонічним `/chat/{slug}` або `?room=` — SW передає в `navigate` |
| Приват | `WebPushService` формує `/chat?private_peer={id}&private_peer_name=…` |
| SPA | `ChatRoom.vue` — `consumePrivatePeerFromRoute()` після bootstrap і в `$route` watcher; після відкриття треду query прибирається через `router.replace` |
| `notificationclick` | Фокус лише на вікні **того ж origin**; `navigate(targetUrl)` + `focus`; інакше `openWindow` |
| Кеш T163 | Лише додано listeners у кастомному `sw.js`, прекеш Workbox без змін |

## Ручний smoke (HTTPS)

1. Увімкнути push (T165), надіслати приват з іншого акаунта → клік по сповіщенню → відкривається чат із панеллю привату з відправником.
2. Повідомлення в кімнаті зі slug → клік → відкривається `/chat/{slug}`.
