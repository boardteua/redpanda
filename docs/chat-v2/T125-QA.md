# T125 — QA (флуд-контроль повідомлень)

## Автоматично

- `php artisan test --filter MessageFloodApiTest` — серія POST, **429** після порогу; дубль `client_message_id` не з’їдає квоту; **VIP** без ліміту.
- `php artisan test --filter ChatSettingsApiTest::test_admin_can_patch_message_flood_settings` — PATCH опцій.
- `npm run build` у `backend/` — **PASS**.

## Ручна перевірка (опційно)

1. Адмін: **Налаштування чату** → вкладка **Команди й модерація** → блок **Антифлуд повідомлень** — увімкнути, задати N і T, зберегти.
2. Звичайний користувач: швидка серія повідомлень у кімнаті — після порогу тост **429** (`rpToastStack`), код у відповіді `message_flood_limit`.
3. Повтор з тим самим `client_message_id` (мережевий retry) — **200** duplicate, без додаткового флуду.
4. **VIP** / модератор / адмін чату — ліміт не застосовується.

## Політика

- Ліміт **глобальний на користувача** (кімната + приват разом), ковзне вікно **T** с, лічильник у **кеші** з TTL.
- **Не** замінює глобальний HTTP throttle з **T04**; додає керований поріг у **ChatSetting**.
