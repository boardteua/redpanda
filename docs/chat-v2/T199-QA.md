# T199 — QA (видалення кімнат, архів)

## Автоматичні перевірки

- `php artisan test tests/Feature/RoomCrudApiTest.php tests/Feature/ChatArchiveApiTest.php`
- `php artisan test tests/Feature/ChatApiTest.php` (узгоджено `Queue::fake` для `BroadcastEvent` після черги broadcast у Laravel)
- `npm run build` (каталог `backend/`)

## Поведінка (коротко)

- `DELETE /api/v1/rooms/{id}`: soft-delete `rooms`; усі рядки `chat` з цієї кімнати без попереднього `archived_from_room_id` отримують `post_deleted_at`, `archived_from_room_id`, `archived_room_name`, `archived_room_access`; slug кімнати змінюється, щоб звільнити ім’я.
- Архів: дописи з видаленої **публічної** кімнати знову видно в `GET /api/v1/archive/messages` з `archived_room_name` (мітка в UI `ArchiveChat`).
- UI: кнопка «Видалити кімнату» за матрицею `roomEditCanDelete` (адмін / модератор / творець + `can_create_room`).

**PASS:** 2026-04-03 (автотести + збірка).
