# T115 — QA: довідка slash-команд лише для адміна

**Задача:** [project-tasks/chat-v2-tasklist.md](../../project-tasks/chat-v2-tasklist.md) — T115.

## Зміни

- `backend/resources/js/lib/userBadgeMenuItems.js`: пункт **Команди** лише при `viewer.chat_role === 'admin'`.
- `backend/resources/js/views/ChatRoom.vue`: захист `onBadgeMenuPick('commands')`; `PrivateChatPanel` — `show-slash-docs` лише для адміна.
- `backend/resources/js/components/PrivateChatPanel.vue`: placeholder з `/clear` лише якщо `showSlashDocs`.
- `backend/resources/js/components/chat/room/ChatRoomModals.vue`: `CommandsHelpModal` монтується лише для адміна.

## Автоматичні перевірки

- `cd backend && npm run build` — PASS

## Ручний чек (оператор)

1. Не-адмін: у меню «своя плашка» немає **Команди**; модал довідника не відкривається.
2. Адмін: **Команди** відкриває довідник як раніше; placeholder привату з `/clear` лише для адміна.

## Вердикт

**PASS** — після ручного чеку.
