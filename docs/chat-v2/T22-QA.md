# T22 — QA (контекстне меню плашки користувача)

## Що зроблено

- Меню дій у сайдбарі (після **T27**): інлайн-панель **`UserBadgeInlineActionPanel`** під рядком користувача (референс board.te.ua), **Escape** і клік поза панеллю/тригером, стрілки **Up/Down** / **Home** / **End**, повернення фокусу на hamburger; матриця пунктів у **`userBadgeMenuItems.js`**.
- **Своя плашка:** Інформація, Команди (модал з **`backend/resources/markdown/board-te-ua-commands.md`** як `?raw`, узгоджено з **`docs/board-te-ua/commands.md`**), **Налаштування чату** — лише `chat_role === admin` (заглушка `SimpleStubModal`), **Профіль** — лише не-гість (заглушка під T24).
- **Чужа плашка:** Інформація, Приват, Ігнор, Додати до друзів (пункт **прихований**, якщо **ціль — гість**; гість-переглядач **може** бачити пункт для зареєстрованої цілі), блок **Кляп / Вигнати** для **moderator** та **admin** (`POST /api/v1/mod/users/{id}/mute|kick` з `window.prompt` для хвилин).
- **Інформація:** модал **`UserInfoModal`** — обмежений текст для **гостя** при перегляді **не** staff; для **staff** + **гість-ціль** — секція-заглушка під **T23** (IP-checker).
- Тригери: вкладка **Люди** (ви + онлайн), **Друзі** / **Ігнор** (hamburger); **Приват** — без меню в списку розмов (відкриття треду з рядка); стрічка повідомлень (аватар і нік — без цього меню, T25).
- **Бекенд:** `FriendController::store` — **422**, якщо ціль `guest` (`test_cannot_send_friend_request_to_guest`).

## Автоматичні перевірки

```bash
cd backend && npm run build
cd backend && php artisan test
```

Очікування (на момент проходження): збірка без помилок; усі тести PASS (включно з `FriendApiTest::test_cannot_send_friend_request_to_guest`).

## Матриця видимості (коротко)

| Переглядач | Ціль | Друзі в меню | Профіль (своє меню) | Налаштування чату |
|------------|------|--------------|---------------------|-------------------|
| Гість | Зареєстрований | Так | — | — |
| Гість | Гість | Ні | — | — |
| Зареєстрований | Гість | Ні | Так (не гість) | Лише admin |
| Staff | Будь-яка (≠ я) | Ні, якщо ціль гість | — | Лише admin |

## Ручна перевірка (опційно)

1. Два акаунти; у сайдбарі клік по плашці онлайн — приват, ігнор, друзі (якщо застосовно).
2. Модератор: кляп/вигнання з prompt; не-мод — пунктів немає.
3. Гість у стрічці: інфо про гостя без зайвих даних (не staff).

## Вердикт

- **PASS (автоматичні):** `npm run build` OK; `php artisan test` — 84 тести OK.
