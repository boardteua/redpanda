# T44 — QA: створення кімнат (права, опис, API, UI)

## Вердикт

**PASS** (автоматизовані перевірки + збірка фронту).

## Докази

- **PHPUnit:** `php artisan test` — усі тести зелені; для T44 додано `Tests\Feature\RoomCreateApiTest` (гість 403, недостатня кількість повідомлень + `code`, успіх після порогу, VIP/мод без порогу, `default_room_only`, видалені повідомлення не лічаться, `can_create_room` у `GET /api/v1/auth/user`).
- **Збірка SPA:** `npm run build` — без помилок.

## Короткий зміст реалізації

- `POST /api/v1/rooms` (throttle `room-create`, 10/хв на користувача): назва + опційний опис (`topic`); нова кімната завжди публічна (`access = 0`).
- Право: `RoomPolicy::create` + `UserPublicMessageCountService` узгоджено з **T51** (`ChatSetting`).
- UI: вкладка «Кімнати» у сайдбарі — форма або пояснення з порогом **N** з `GET /chat/settings`.

## Ручна перевірка (опційно для оператора)

1. Зареєстрований користувач без порогу — бачить пояснення; після достатньої кількості публічних повідомлень — форма та успішне створення.
2. Гість — блоку створення на бекенді; форма не показується.
