# T135 — QA: розділення CSS вітальні / чату

**Статус:** **PASS** (2026-03-25).

## Збірка

- `cd backend && npm run build` — PASS.
- `php artisan test` — PASS (375 tests).

## Розміри CSS (vite build, gzip)

| Asset | Роль | Розмір (gzip) |
|-------|------|---------------|
| `welcome-*.css` | Перший paint для `/`, auth, forgot/reset | ~87 kB (~17 kB) |
| `chat-*.css` | Deep link `/chat`, `/archive`, staff | ~98 kB (~19 kB) |
| `chat-only-*.css` | Динамічно після welcome → чат | ~9 kB (~2 kB) |

Вітальня не тягне `rp-chat-chrome` у першому бандлі; перехід на чат підвантажує лише `chat-only.css` (JS `import()`).

## Трасування

- Entry: `backend/resources/css/welcome.css`, `chat.css`, `chat-only.css`.
- Частини: `backend/resources/css/partials/`.
- Blade: `resources/views/spa.blade.php` — вибір entry за шляхом; `window.__RP_INITIAL_CSS_ENTRY__`.
- Router: `resources/js/router/index.js` — `beforeEach` + lazy `chat-only.css`.
