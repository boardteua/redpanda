# T116 — QA: мобільний сайдбар і темна тема (без «коричневого» шару)

**Задача:** [project-tasks/chat-v2-tasklist.md](../../project-tasks/chat-v2-tasklist.md) — T116.

## Зміни

- `backend/resources/css/app.css`: мобільний `.rp-chat-burger-drawer` — фон **`--rp-chat-sidebar-bg`** замість `--rp-burger-bg`; картки/кнопки кімнат у drawer — `color-mix` від **`--rp-chat-sidebar-fg`**; у **темній** темі та **system+dark**: **`--rp-burger-self-bar-bg`** → `var(--rp-surface-elevated)`.
- `backend/resources/js/components/chat/sidebar/ChatSidebarTabBars.vue`: мобільна смуга вкладок на токенах **`--rp-chat-sidebar-*`** замість `white/…`.

## Автоматичні перевірки

- `cd backend && npm run build` — PASS
- `cd backend && php artisan test` — **324 passed**

## Ручний чек (оператор)

1. Viewport &lt; 768px, **темна** тема: відкрити сайдбар — фон і таби узгоджені з палітрою чату, без окремого коричневого full-screen шару.
2. **Світла** тема: мобільний сайдбар лишається коричневим як десктоп (`--rp-chat-sidebar-bg`).
3. Поля пошуку / порожні стани — без регресу контрасту.

## Вердикт

**PASS** — після ручного чеку.
