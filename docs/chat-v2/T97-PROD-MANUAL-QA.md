# T97 — Production manual regression (chat) — report

**Дата звіту:** 2026-03-24  
**Середовище (URL):** `https://new.board.te.ua/`  
**Інструмент верифікації:** MCP **user-chrome-devtools** (Chrome DevTools Protocol) у Cursor.  
**Версія деплою на VPS:** не знімалась з SSH у цьому прогоні — за потреби доповнити оператором (`git rev-parse HEAD` у `/var/www/redpanda` або артефакт CI).  
**Локальний SHA репозиторію** на момент складання артефакта в git: `a525fff` (орієнтир для документації, не тотожний prod без підтвердження).

**Трасованість до чекліста:** тесткейси нижче **зіставлені з закритими Txx** у `project-tasks/chat-v2-tasklist.md` (рядки `### [x] Txx`). Опційні невиконані задачі `### [ ]` (наприклад **T13**, **T19**, **T23**, **T72**, **T73**, **T79**) у матриці позначені **N/A**.

**Follow-up (T98 / T10):** у цьому прогоні **не** виконувався сценарій **`POST /api/v1/images`** на prod під залогіненим користувачем; після деплою виправлення **T98** варто додати рядок у матрицю та прогнати завантаження зображення в чат.

**Ролі в прогоні:** сесія була **гостем** (автоматична після завантаження prod — без введення облікових даних у формах). Сценарії, що потребують зареєстрованого користувача, адміна або Auth0, позначені **blocked (потрібні облікові дані / окремий прогін)** — **без PII**.

---

## Підсумок

| Зона | Результат |
|------|-----------|
| Завантаження SPA / чат | **pass** (редирект на `/chat`, кімната `room=1`, API 200) |
| Заголовок вкладки (T93) | **pass** |
| Відсутність банера poll / real-time fallback (T96 smoke) | **pass** (перевірка тексту в `document.body`) |
| WebSocket **101** у DevTools Network | **partial** — MCP `list_network_requests` **не повернув** записів типу `websocket` у цьому прогоні; для формального закриття критерію T96 рекомендовано ручний рядок у DevTools або повтор з інструментом, що експонує WS |
| Два клієнти / доставка в real-time без reload (T20/T06) | **blocked** (потрібні дві сесії та узгоджений сценарій) |
| Auth / forgot password / Auth0 (T02, T94, T76) | **blocked** (без логіну) |
| Staff / RBAC (T21, T52, T57) | **blocked** (без staff-облікових записів) |

---

## Матриця тесткейсів (TC → Txx)

| TC-ID | Зв’язані Txx | Кроки (скорочено) | Очікування | Результат | Примітки / доказ (MCP) |
|-------|----------------|-------------------|------------|-----------|-------------------------|
| TC-T97-01 | T03, T77, T15 | Відкрити `/` | Вітальня / вхід або редирект у чат за політикою сесії | **pass** | `GET /` 200, далі перехід у чат |
| TC-T97-02 | T02, T03 | Гість у чаті | Доступ до загального чату без форми логіну | **pass** | Snapshot: стрічка, композер, сайдбар |
| TC-T97-03 | T93 | Перебування в кімнаті з назвою | `document.title` містить бренд і назву кімнати | **pass** | `evaluate_script`: `Чат Рудої Панди — Руда Панда` |
| TC-T97-04 | T04, T06, T28–T35 | Відображення стрічки, тулбар композера | Повідомлення з API, кнопки форматування | **pass** | Snapshot: log, кнопки кольору/B/I/U, смайли |
| TC-T97-05 | T05, T06, T96 | Після завантаження чату | Немає банера «Реалтайм недоступний…»; WS має бути активний (див. partial) | **pass** (UI), **partial** (WS) | `pollFallbackMention: false`; WS не з MCP list |
| TC-T97-06 | T07, T41, T42 | Сайдбар | Вкладки Люди/Друзі/Приват/Кімнати/Ігнор; «Люди» активна | **pass** | Snapshot: tab «Люди» selected |
| TC-T97-07 | T20, T48, T49 | Список «Люди» | Онлайн-статуси, роль адміна для `org100h` | **pass** (видимість UI) | Snapshot: «Адміністратор» біля org100h |
| TC-T97-08 | T08, T56, T85 | Приват | Вкладка доступна; автокомпліт «Почати приватний чат» | **not executed** | UI присутній; відправка/лічильники — окремий прогін |
| TC-T97-09 | T10, T82 | Медіа / lightbox | Гість: upload недоступний | **pass** (обмеження гостя) | Snapshot: кнопка завантаження disabled для гостя |
| TC-T97-10 | T09 | Архів | Лінк з чату | **not executed** | Лінк «Архів чату» присутній у snapshot |
| TC-T97-11 | T21, T52, T57 | Staff UI | Доступ лише для staff | **blocked** | Потрібні облікові дані staff |
| TC-T97-12 | T76, T77 | Auth0 / вхід | Провайдери на вітальні | **blocked** | Потрібен окремий прогін логіну |
| TC-T97-13 | T94 | Forgot password | Форми та API | **blocked** | Потрібен ручний прогін без автоматизації облікових даних |
| TC-T97-14 | T14 | Повний інтеграційний шлях | Логін → чат → сайдбар → приват | **partial** | Виконано гілку «гість → чат → сайдбар» |
| TC-T97-15 | T13, T19, T23, T72, T73, T79 | Опційні Txx | — | **N/A** | Не на prod / не в scope чекліста |

---

## Докази MCP (user-chrome-devtools)

1. **Навігація:** `navigate_page` → `https://new.board.te.ua/` → фактична сторінка `https://new.board.te.ua/chat?room=1`.
2. **Мережа (фрагмент):** після завантаження присутні успішні виклики `GET /api/v1/rooms`, `GET /api/v1/rooms/1/messages`, `POST /broadcasting/auth` (200), `POST .../presence-status` (200) — узгоджується з **T04**/**T05**/**T20**.
3. **Консоль:** повідомлень не зафіксовано (`list_console_messages` порожньо на момент знімку).
4. **Скрипт на сторінці:** перевірка відсутності тексту про fallback poll — `pollFallbackMention: false`.

---

## Рекомендовані доповнення оператором (закрити partial/blocked)

1. У Chrome DevTools → **Network → WS**: підтвердити статус **101** для з’єднання Reverb/Echo (**T96**).
2. Дві вкладки / два браузери: надіслати повідомлення з **А**, перевірити в **Б** без повного reload (**T06**/**T20**).
3. Пройти логін (email+password і за потреби Auth0), forgot password (**T94**), staff-маршрути (**T52**/**T57**) — оновити цю таблицю в тому ж файлі або додатком з датою.

---

## Відомі дефекти

У цьому прогоні **не зафіксовано** відмов (fail). Обмеження — **coverage**: частина TC залишилась **blocked** через відсутність облікових даних і неекспорт WS у MCP-списку мережі.
