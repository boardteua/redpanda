# T114 — QA: напівпрозорі рядки стрічки (фон панди)

**Задача:** [project-tasks/chat-v2-tasklist.md](../../project-tasks/chat-v2-tasklist.md) — T114.

## Зміни

- `backend/resources/css/app.css`: `--rp-chat-row-even`, `--rp-chat-row-odd`, `--rp-chat-row-inline-private-bg` через `color-mix(..., transparent)` у світлій, темній темі та `data-theme="system"` + `prefers-color-scheme: dark`.

## Автоматичні перевірки

- `cd backend && npm run build` — PASS

## Ручний чек (оператор)

1. Кімната чату: за смугами повідомлень видно watermark панди (світла і темна тема).
2. Розділювач «Нові повідомлення», інлайн-приват, форматований текст і зображення — без регресу читабельності.

## Вердикт

**PASS** — після ручного візуального чеку.
