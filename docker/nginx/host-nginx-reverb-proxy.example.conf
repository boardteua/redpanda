# Фрагмент для хостового nginx (TLS на 443), коли Laravel HTTP у Docker :8080, Reverb — :6001.
# Документація Reverb: WebSocket на /app/*, HTTP API на /apps/* — обидва шляхи мають йти на Reverb.
# Підставте server_name та шляхи до сертифікатів (Certbot).
#
#     map $http_upgrade $connection_upgrade {
#         default upgrade;
#         ''      close;
#     }
#
#     server {
#         server_name new.board.te.ua;
#         ...
#
#         location / {
#             proxy_http_version 1.1;
#             proxy_pass http://127.0.0.1:8080;
#             proxy_set_header Host              $host;
#             proxy_set_header X-Real-IP         $remote_addr;
#             proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#             proxy_read_timeout  300s;
#         }
#
#         location /app {
#             proxy_http_version 1.1;
#             proxy_set_header Host              $host;
#             proxy_set_header X-Real-IP         $remote_addr;
#             proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#             proxy_set_header Upgrade           $http_upgrade;
#             proxy_set_header Connection        $connection_upgrade;
#             proxy_buffering off;
#             proxy_pass http://127.0.0.1:6001;
#             proxy_read_timeout  86400s;
#             proxy_send_timeout  86400s;
#         }
#
#         location /apps {
#             proxy_http_version 1.1;
#             proxy_set_header Host              $host;
#             proxy_set_header X-Real-IP         $remote_addr;
#             proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#             proxy_set_header Upgrade           $http_upgrade;
#             proxy_set_header Connection        $connection_upgrade;
#             proxy_buffering off;
#             proxy_pass http://127.0.0.1:6001;
#             proxy_read_timeout  86400s;
#             proxy_send_timeout  86400s;
#         }
#     }
