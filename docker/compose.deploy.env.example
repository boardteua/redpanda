# Legacy: лише MYSQL_* / REDIS_PASSWORD для --env-file (без повного Laravel-набору).
# Канон — docker/production.env.example → docker/production.env (той самий compose.yaml).

MYSQL_ROOT_PASSWORD=
MYSQL_PASSWORD=
REDIS_PASSWORD=
