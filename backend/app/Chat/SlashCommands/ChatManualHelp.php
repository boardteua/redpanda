<?php

namespace App\Chat\SlashCommands;

/**
 * Статичний текст для /manual (T67); повний реєстр — docs/board-te-ua/commands.md.
 */
final class ChatManualHelp
{
    public static function text(): string
    {
        return implode("\n", [
            'Короткий довідник команд чату:',
            '/manual — показати цей текст',
            '/away — перемкнути «відійшов» / «онлайн» у поточній кімнаті',
            '/me текст — дія / системне повідомлення в стрічці',
            '/seen нік — остання активність у цій кімнаті та статус присутності',
            '/msg нік текст — надіслати особисте (інлайн у стрічці)',
            '/friend нік — запит на додавання до друзів (лише зареєстровані)',
            '/ignore нік — додати до ігнору (лише зареєстровані); /ignoreclear — очистити список ігнору',
            'У вікні привату: /clear — видалити історію треду з поточним співрозмовником',
            'Команди модерації та адміна — /topic, /clear у кімнаті, /mute, … (див. повний список у документації).',
        ]);
    }
}
